# 部署指南 (Deployment Guide)

## 前置要求 (Prerequisites)
- Docker
- Docker Compose

## 快速开始 (Quick Start)

### 1. 启动服务
在项目根目录下运行：
```bash
docker-compose up -d
```

### 2. 验证安装
访问以下地址确认服务是否正常：
- 后端 API: `http://localhost:5000`
- 前端应用: `http://localhost:3000` (开发模式)
