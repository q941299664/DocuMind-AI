server:
  port: 8080

spring:
  application:
    name: documind
  profiles:
    active: local

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

mpp:
  entityBasePath: top.demotao.documind.entity
